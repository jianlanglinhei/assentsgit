layers:
  - name: stable
    path: MEMORY.md
    max_lines: 120
    format: markdown

  - name: episodes
    path: episodes/
    format: yaml
    rotation: monthly

update_triggers:
  - on_session_end
  - on_explicit_save

archive_policy:
  max_entries: 5000
  compress_after: 90d
